apiVersion: apps/v1
kind: Deployment
metadata:
  name: patient-service
spec:
  replicas: 2  # Number of replicas (adjust as needed)
  selector:
    matchLabels:
      app.kubernetes.io/name: patient-service  # Ensure this matches the label in the pod template
  template:
    metadata:
      labels:
        app.kubernetes.io/name: patient-service  # Same label as the selector
    spec:
      containers:
        - name: patient-service
          image: bhagya95/patient-service:latest  # Replace with your Docker Hub image
          ports:
            - containerPort: 3003
          env:
            - name: URI
              valueFrom:
                secretKeyRef:
                  name: patient-service-secret
                  key: URI
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: patient-service-secret
                  key: JWT_SECRET
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: patient-service-config
                  key: PORT
            - name: AUTH_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: patient-service-config
                  key: AUTH_SERVICE_URL

                